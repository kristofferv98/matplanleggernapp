import SwiftUI

struct SolidBackground: View {
    var body: some View {
        Color(red: 0.75, green: 0.75, blue: 0.75) // Replace these values with the RGB values of your desired color
            .edgesIgnoringSafeArea(.all)
    }
}
